### Go programs
Repo contains coding problems and their respective solutions in golang.

Each solution is encapsulated in individual file.

Note: Ignore `package main` and redundant `main` function declarations in each file, often detected as warnings/errors.